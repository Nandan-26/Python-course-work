function- it is a block of reusable code,used for specific task
uses:
1.makes debugging easy when ever needed
2.reduce re-usage of same code
3.code reusability
4.breaking down big problems to smaller code
"""A function is a reusable block of code that performs a specific task.

Functions can accept inputs called parameters and can return an output.
"""


def greet(name):
	"""Return a greeting for the given name."""
	return f"Hello, {name}!"


print(greet("Eswar"))
